<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.eci.cvds.persistence.mybatis.mappers.ElementoMapper">
    <select parameterType="map" id="consultarElementos" resultMap="ElementoResult" >
        select
            c.idElemento,
            c.tipo,
            c.nombre,
            c.descripcion
        FROM Elemento as c
    </select>

    <insert id="registrarElemento" parameterType="map">
        INSERT INTO Elemento(tipo,nombre,descripcion) VALUES(#{Elemento.tipo}, #{Elemento.nombre} , #{Elemento.descripción});
    </insert>

    <resultMap id="ElementoResult" type="Elemento">
        <id property="idElemento" column="idElemento"/>
        <result property="tipo" column="tipo"/>
        <result property="nombre" column="nombre"/>
        <result property="descripcion" column="descripcion"/>
    </resultMap>

</mapper>